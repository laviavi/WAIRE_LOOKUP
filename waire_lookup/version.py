# Version tracking for the WAIRE LookUp Tool.
#
# Bump on every iteration:
#   SERVER_VERSION — backend changes (app.py, connectors, core logic, routes)
#   UI_VERSION     — frontend changes (templates, static CSS/JS)
#
# Record every bump in CHANGELOG.md with a one-line note.

SERVER_VERSION = "1.24.0"
UI_VERSION = "2.34.0"
RELEASE_VERSION = "1.3.1"
